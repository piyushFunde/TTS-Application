spring.application.name=tts-backend
server.port=8080
spring.mvc.problemdetails.enabled=false
# Configure these in your environment, never in source control.
tts.api-key=${TTS_API_KEY:}
tts.region=${TTS_REGION:}
tts.endpoint=${TTS_ENDPOINT:}
